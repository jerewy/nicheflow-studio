# UI Framework
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
# Webview shell for the incremental React UI migration (docs/UI_MIGRATION_PLAN.md).
# Hosts the Vite/React Processing screen in an OS WebView2 window and exposes the
# Python draft-revision service through an in-process bridge. Runs alongside the
# existing PyQt app until the migrated slice is validated.
pywebview==5.3.2

# Database
SQLAlchemy==2.0.25

# Video Processing
yt-dlp==2026.3.17
instaloader==4.15
instagrapi==2.5.15
playwright==1.56.0
faster-whisper==1.2.1
# Face detection for auto-picking reel covers (Haar cascade ships inside the
# wheel — no extra model file). HEADLESS build on purpose: the GUI build bundles
# its own Qt, which clashes with PyQt6. Cover picking degrades gracefully to
# sharpness/exposure scoring if this import is ever unavailable.
opencv-python-headless==4.13.0.92

# Apify (alternative IG scraping path — no IG accounts required, pay-per-result)
apify-client==1.8.1
apify-shared<2

# Upload Integrations
google-api-python-client==2.169.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# Development
pytest==7.4.3
black==23.12.1
flake8==6.1.0
watchfiles==1.1.1
pyinstaller==6.19.0
